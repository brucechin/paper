enclave {
	
	trusted {
        /* define ECALLs here. */
        public int ecall_dummy(int i);

        /* =========================== START SOLUTION =========================== */
        /* ============================ END SOLUTION ============================ */
    };
	
	untrusted {
        /* define OCALLs here. */
        void ocall_print([in,string] const char *str);
	};
};
